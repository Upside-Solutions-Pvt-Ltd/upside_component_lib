import 'package:flutter/material.dart';

class AppBarWithDrawer extends StatelessWidget {
  const AppBarWithDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(title: const Text('Label'));
  }
}
